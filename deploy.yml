name: Deploy Calculator Website

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Deployment Success
        run: echo "Calculator Website Deployed Successfully"